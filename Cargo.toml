[package]
name = "copyall"
version = "0.1.0"
edition = "2024"

[dependencies]
clap = {version = "4.5.40", features = ["derive"]}
regex = "1.11.1"
serde = {version = "1.0", features = ["derive"]}
serde_json = "1.0"
